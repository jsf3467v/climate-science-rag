# Serving dependencies for the climate-arXiv RAG Space (app.py + src/).
# scikit-learn is here only for the frozen ENGLISH_STOP_WORDS the tokenizer
# uses; do not drop or swap it, or query tokenization changes and the measured
# numbers no longer hold. Build-time tools (corpus/full-text fetch) are not
# served and live in requirements-build.txt.
anthropic==0.106.0
gradio==6.18.0
numpy~=2.4
scikit-learn==1.8.0
rank_bm25==0.2.2
joblib~=1.5